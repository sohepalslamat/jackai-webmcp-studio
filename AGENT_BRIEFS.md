# الخطة الموسّعة — WebMCP Challenge

**المشروع:** استوديو بناء مساعدين، يعمل عليه الإنسان ووكيله معًا، وكل فعل حسّاس فيه محكوم ببوابة موافقة حتمية.
**النطاق:** `studio.jack-ai.net` (فرعي — لا تمسّ الموقع الرئيسي)
**المستودع:** جديد، عام، MIT، والترخيص ظاهر في قسم About
**الموعد:** الأربعاء ٢ سبتمبر مساءً (الحدّ الرسمي ٣ سبتمبر ١١ ليلًا بتوقيت إسطنبول — لا تقترب منه)

---

## قبل تشغيل أي وكيل — أنت وحدك

نصف ساعة غير قابلة للتفويض. لا تفتح مسارًا واحدًا قبل أن تنتهي منها:

1. مستودع جديد + `LICENSE` (MIT) في الجذر، وتأكّد أنّ GitHub يعرضه في About.
2. Next.js على النطاق الفرعي، HTTPS يعمل.
3. **لا تضبط** ترويسة `Origin-Agent-Cluster: ?0` — WebMCP يعمل في المستندات معزولة الأصل فقط.
4. أداة تجريبية واحدة، ثم افتح الصفحة في متصفح ChatGPT المدمج واطلب استدعاءها. **إن لم يصل الاستدعاء، كل ما بعده هدر.**
5. الصق `lib/contracts.ts` و`lib/consent/` كما هي، وشغّل الاختبارات: `vitest run`. يجب أن تمرّ الأربعة قبل أن يبدأ أحد.

الاسم `document.modelContext` لا `navigator.modelContext` — القديم مهجور من كروم ١٥٠.

---

## قواعد التوازي

- **`lib/contracts.ts` للقراءة فقط.** أي مسار يعدّله يكسر البقية. أي حاجة للتغيير تمرّ عليك أنت.
- كل مسار يملك ملفاته وحده. لا أحد يكتب في ملفات غيره.
- التكامل بين المسارات يمرّ عبر الأنواع في `contracts.ts` وواجهة `Store` في `lib/tools/register.ts` — لا عبر تنسيق شفهي.
- كل مسار يفتح فرعًا ويدمج على `main` بعد نجاح الاختبارات.

---

## ملكية الملفات

| المسار | يملك | ممنوع عليه |
|---|---|---|
| أ — نواة المنتج | `app/**`, `components/studio/**`, `lib/store.ts` | `lib/contracts.ts`, `lib/consent/**` |
| ب — الأدوات | `lib/tools/**` | `lib/contracts.ts`, `lib/consent/**` |
| ج — البوابة | `lib/consent/**`, `components/ConsentDialog.tsx` | `app/**` |
| د — لوحة العرض | `components/AgentPanel.tsx` | كل ما عداه |
| هـ — التسليم | `README.md`, `SUBMISSION.md`, `docs/**` | الكود |

---

## بريف المسار أ — نواة المنتج

> ابنِ استوديو بناء مساعدين ذكاء اصطناعي بـNext.js (App Router) وTypeScript وTailwind، بواجهة عربية RTL مع تبديل للإنجليزية.
>
> **شرط قاطع: التطبيق يعمل كاملًا للإنسان وحده، بلا أي وكيل.** WebMCP تحسينٌ فوقه لا شرطٌ له. إن كانت أي شاشة بلا معنى من دون وكيل، فقد أخطأت.
>
> الشاشات: قائمة المساعدين، إنشاء مساعد (اسم، غرض، نبرة، لغة)، صفحة مساعد فيها قاعدة المعرفة وصندوق تجربة، وأزرار نشر ومشاركة وحذف.
>
> نفّذ `lib/store.ts` مطابقًا لواجهة `Store` في `lib/tools/register.ts` حرفيًا — نفس أسماء الدوال ونفس التواقيع. الحالة في الذاكرة (Zustand أو useSyncExternalStore)، بلا قاعدة بيانات، بلا مصادقة.
>
> **مهم:** أزرار النشر والمشاركة والحذف في الواجهة تستدعي `store` مباشرة. لا تمرّ من البوابة — البوابة للوكيل لا للإنسان، والإنسان حاضرٌ بنقرته أصلًا.
>
> الفراغ دعوةٌ للفعل لا رسالةُ اعتذار: شاشة «لا مساعدين بعد» تعرض زر إنشاء وثلاثة أمثلة جاهزة.

## بريف المسار ب — أدوات WebMCP

> اكتمل `lib/tools/register.ts` في المستودع. مهمّتك تشغيله وإتقانه، لا إعادة كتابته.
>
> اربطه بـ`lib/store.ts` من المسار أ، واستدعِ `registerTools` من مكوّن عميل يعمل مرة واحدة عند التركيب، وألغِ التسجيل عند الفكّ بالدالة المُعادة.
>
> اجعل تسجيل `publish_assistant` و`share_assistant` و`delete_assistant` **مشروطًا بوجود مساعد واحد على الأقل**: سجّلها عند أول إنشاء وألغِها عند حذف الأخير. هذا يُظهر بُعد الحالة في المعيار ويولّد حدث `toolchange` حقيقيًا يلتقطه المسار د.
>
> مرّر `signal` إلى كل عملية غير متزامنة داخل `execute`.
>
> **ممنوع منعًا باتًّا:** إنشاء أي أداة تمنح موافقة أو تؤكّد أو تتخطّى البوابة، بأي اسم كان. وممنوع استدعاء `gate.grant` من هذا الملف. إن بدا لك أنّ الوكيل «عالق» أمام البوابة، فهذا هو السلوك الصحيح.
>
> اختبر في متصفح ChatGPT المدمج وفي كروم عبر `chrome://flags/#enable-webmcp-testing`.

## بريف المسار ج — البوابة

> `lib/contracts.ts` و`lib/consent/ledger.ts` و`ConsentProvider.tsx` و`ConsentDialog.tsx` مكتوبة. مهمّتك تصليبها.
>
> شغّل `lib/consent/ledger.test.ts` وتأكّد من مرور الأربعة. ثم **أضف اختبارات هجوم**: نموذج معادٍ يستدعي الأداة الحسّاسة عشرين مرة متتالية، ويستدعيها بوسائط مرتّبة عكسيًا، ويحاول استهلاك موافقة بعد انتهاء مهلتها بمللي ثانية واحدة، ويحاول استدعاء `consume` مباشرة بتجزئة مخمّنة.
>
> راجع مسارات الالتفاف: هل يوجد أي طريق يصل إلى `grant` غير `onClick` في `ConsentDialog`؟ ابحث في المستودع كله عن `grant(` وتأكّد.
>
> اضبط الوصولية في النافذة: حبس التركيز داخلها، إغلاق بـEscape يساوي رفضًا، `aria-modal`, وتباين كافٍ.
>
> **لا تغيّر `TOOL_MANIFEST` ولا تجعل الحساسية قابلة للتمرير كوسيط.** هذه هي النقطة كلها.

## بريف المسار د — لوحة العرض

> `components/AgentPanel.tsx` جاهز. مهمّتك جعله مقروءًا في فيديو بدقة ١٠٨٠p ومدة ثلاث دقائق.
>
> ثلاثة أقسام: الأدوات المسجّلة الآن (مع وسم «يحتاج تأكيد» على الحسّاسة)، وقرارات البوابة، وآخر الاستدعاءات.
>
> **الأهم:** حين يُوقف استدعاءٌ، أبرزه بوضوح — حركة قصيرة، لون مختلف، سطر يقول ما حدث بلغة المستخدم لا بلغة النظام. «أُوقف: هذا الفعل يحتاج تأكيدك» لا «CONSENT_REQUIRED». احترم `prefers-reduced-motion`.
>
> أظهر عدّاد المهلة على الموافقة السارية — المُشاهد يفهم منه أنّ الموافقة معاصرة لا دائمة.
>
> اللوحة ثابتة على اليسار في الشاشات العريضة، وقابلة للطيّ على الموبايل.

## بريف المسار هـ — مواد التسليم

> اكتب `README.md`: ما هو المشروع، كيف يُشغَّل محليًا، كيف يُختبر في متصفح ChatGPT المدمج، وقسم معماري يشرح البوابة بمخطط بسيط.
>
> اكتب `SUBMISSION.md` بالإنجليزية يجيب الأسئلة الأربعة المطلوبة: لماذا الحالة مناسبة لـWebMCP، كيف تحسّن التجربة، ماذا صار ممكنًا لم يكن كذلك، وكيف نُفّذت WebMCP.
>
> اربط الورقة: `doi.org/10.5281/zenodo.20847425` — وقل صراحةً إنّ هذا التطبيق تجسيدٌ للحارس الحتمي فيها.
>
> **صراحة إلزامية:** اكتب أنّ هذا لا يحلّ حقن التلقين، وأنّ المضمون شيئان فقط: لا فعل حسّاس بلا تأكيد معاصر مربوط بالفعل، والضمان قائم خارج النموذج. لا تبالغ.

---

## اختبارات القبول — بوابة الدمج

لا يُدمج شيء على `main` قبل مرور هذه:

1. فعل حسّاس بلا موافقة يُسقَط، ولا يُنفَّذ شيء.
2. الفعل نفسه بموافقة مطابقة يمرّ.
3. موافقة على فعل لا تمرّر فعلًا آخر — لا بأداة أخرى ولا بوسائط أخرى.
4. الرمز المستهلَك لا يُعاد استعماله.

الأربعة هي ثوابت §7.4 في الورقة. مرورها يعني أنّ التطبيق تجسيدٌ للحارس لا محاكاةٌ له.

---

## سيناريو الفيديو — أقل من ٣ دقائق، عام على يوتيوب، بصوت

**٠:٠٠–٠:٢٠** الشاشة والجملة الافتتاحية: استوديو يبني فيه الإنسان مساعدًا، ووكيله يعمل معه على السطح نفسه.

**٠:٢٠–١:١٠** الوكيل ينشئ مساعدًا ويضيف معرفة ويجرّبه. الأدوات المنخفضة تمرّ بسلاسة، واللوحة تُظهر الاستدعاءات تصل.

**١:١٠–٢:٠٠** «انشره على واتساب». البوابة توقف الاستدعاء. اللوحة تُبرز الإيقاف. نافذة التأكيد تعرض الفعل بالضبط وبصمته. نقرة. ينفَّذ. عدّاد المهلة يظهر.

**٢:٠٠–٢:٣٠** اللحظة الحاسمة: الوكيل يحاول تمرير الموافقة نفسها لحذف مساعد آخر. رفض. ثم يحاول إعادة استعمال الرمز المستهلَك للفعل نفسه. رفض.

**٢:٣٠–٢:٥٠** جملة واحدة: هذا تجسيدٌ لحارس حتمي منشور على Zenodo، والضمان قائم خارج النموذج لا داخله. رقم الـDOI على الشاشة.

**٢:٥٠–٣:٠٠** لا يحلّ حقن التلقين، ولا يدّعي ذلك.

---

## قائمة ما يُسقط التسليم

- [ ] المستودع عام والترخيص ظاهر في About
- [ ] الرابط الحيّ يفتح ويعمل في متصفح ChatGPT المدمج من جهاز غير جهازك
- [ ] الفيديو عام على يوتيوب، أقل من ٣ دقائق، بصوت
- [ ] الوصف يجيب الأسئلة الأربعة
- [ ] تعليمات التشغيل في README تعمل على مستودع نظيف
- [ ] لا مفاتيح ولا أسرار ولا بيانات عملاء في التاريخ
- [ ] الأهلية: بلد الإقامة تركيا لا سوريا — راجع صفحة الشروط بنفسك
- [ ] سُلِّم يوم ٢ سبتمبر، لا ٣
