# studio.jack-ai.net — WebMCP Challenge

استوديو بناء مساعدين، يعمل عليه الإنسان ووكيله معًا. كل فعل حسّاس محكوم ببوابة
موافقة حتمية تعمل **خارج النموذج**. الخطة الكاملة وبريفات المسارات في
`AGENT_BRIEFS.md` — اقرأه قبل أي مهمة.

**الموعد النهائي: ٢ سبتمبر ٢٠٢٦ مساءً.** لا تبدأ عملًا لا يُنهى قبله.

## قواعد لا تُخرق

1. **`lib/contracts.ts` للقراءة فقط.** لا تعدّله ولا تعد كتابته ولا «ترتّبه».
   إن ظننتَ أنك تحتاج تعديله، توقّف واسأل المستخدم. الحساسية فيه ثابتة وقت
   البناء عمدًا.

2. **الحساسية لا تُمرَّر كوسيط.** تُقرأ من `TOOL_MANIFEST` فقط. أي كود يقرأ
   الحساسية من `args` أو من استدعاء الأداة هو خطأ أمني لا تحسين.

3. **لا توجد أداة تمنح موافقة.** لا `approve_action` ولا `confirm` ولا
   `grant_consent` ولا أي اسم آخر. ولا يُستدعى `gate.grant` إلا من `onClick`
   في `components/ConsentDialog.tsx`.

4. **البوابة تسقط مغلقة.** غياب الموافقة يعني عدم التنفيذ، لا تنفيذًا مع
   تحذير. لا تضف مسار تجاوز ولو خلف علم تطوير.

5. **`vitest run` يمرّ قبل أي commit.** الاختبارات الأربعة في
   `lib/consent/ledger.test.ts` هي عقد المشروع. لا تعدّلها لتمرّ — عدّل الكود.

6. **لا أسرار في المستودع.** لا مفاتيح، لا بيانات عملاء، لا شيء من الموقع
   الرئيسي. المستودع عام.

## ملكية الملفات

لا تكتب خارج ملفات مسارك:

| المسار | يملك |
|---|---|
| أ — نواة المنتج | `app/**`, `components/studio/**`, `lib/store.ts` |
| ب — الأدوات | `lib/tools/**` |
| ج — البوابة | `lib/consent/**`, `components/ConsentDialog.tsx` |
| د — لوحة العرض | `components/AgentPanel.tsx` |
| هـ — التسليم | `README.md`, `SUBMISSION.md`, `docs/**` |

## تفاصيل تقنية

- الاسم `document.modelContext` — لا `navigator.modelContext` (مهجور من كروم ١٥٠).
- WebMCP يعمل في المستندات معزولة الأصل فقط. لا تضبط `Origin-Agent-Cluster: ?0`.
- سياسة الأذونات `tools` افتراضها `self`. الإطارات عابرة الأصل تحتاج `allow="tools"`.
- التسجيل عبر `registerTool` مع `AbortSignal` للإلغاء عند فكّ المكوّن.
- الاختبار: متصفح ChatGPT المدمج، أو كروم بعد `chrome://flags/#enable-webmcp-testing`.
- Next.js App Router + TypeScript + Tailwind. الواجهة **إنكليزية فقط** (LTR) — قرار
  المستخدم في ٢٧ أغسطس: لجنة التحكيم إنكليزية، ولا حاجة لتبديل لغات. لغة المساعد
  المبني (ar/en/tr) تبقى خيارًا للمستخدم، وهي محتواه لا نصّ الواجهة.
- الحالة في الذاكرة. لا قاعدة بيانات، لا مصادقة، لا `localStorage` للموافقات.

## أوامر

```bash
pnpm dev          # تشغيل محلي
pnpm test         # vitest run
pnpm build        # يجب أن ينجح قبل النشر
```

## أسلوب

- التطبيق يعمل كاملًا للإنسان وحده. WebMCP تحسينٌ فوقه لا شرطٌ له.
- نصوص الواجهة بلغة المستخدم لا بلغة النظام: «هذا الفعل يحتاج تأكيدك» لا
  `CONSENT_REQUIRED`.
- الفراغ دعوةٌ للفعل. الخطأ يقول ما حدث وكيف يُصلَح، ولا يعتذر.
- لا تدّعِ في أي نصّ أنّ هذا يحلّ حقن التلقين. لا يحلّه.
